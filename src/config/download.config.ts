/**
 * APK distribution settings — edit this file only to change the download link.
 * The app is delivered outside the Play Store, so users must allow the install.
 */
export const APK_DOWNLOAD = {
  /** Direct link to the hosted APK file. */
  url: 'https://file.kiwi/b38198a2#Mf6h9x7Pen-KT37Te-vjYw',
  /** Shown next to the button (optional, free text). */
  version: '',
} as const;
